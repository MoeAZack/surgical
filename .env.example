# PORT: Port the Express server listens on. Cloud Run injects this automatically.
PORT="3000"

# DB_PATH: Absolute path to the JSON data store. On Cloud Run, point this at a
# mounted Cloud Storage volume (e.g. /data/db.json) so data survives restarts.
DB_PATH="./db.json"
